<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/purple</item>
        <item name="colorPrimaryDark">@color/purple</item>
        <item name="colorAccent">@color/purple_accent</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/purple_accent</item>
        <item name="colorSecondaryVariant">@color/purple</item>

        <!-- Material 3 sizes a small top app bar at 64dp, which is what Compose's TopAppBar lays
             out to. The remaining XML toolbars all read ?attr/actionBarSize, so overriding it here
             keeps them the same height as the Compose screens. -->
        <item name="actionBarSize">64dp</item>

        <item name="android:actionBarStyle">@style/MyActionBar</item>

        <!-- Support library compatibility -->
        <item name="actionBarStyle">@style/MyActionBar</item>
        <item name="toolbarStyle">@style/ToolbarStyle</item>
    </style>

    <style name="AppTheme.Starting" parent="Theme.SplashScreen">
        <!-- Set the splash screen background, animated icon, and animation duration. -->
        <item name="windowSplashScreenBackground">@color/purple</item>

        <!-- Use windowSplashScreenAnimatedIcon to add either a drawable or an
             animated drawable. One of these is required. -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_adaptive_fore</item>
        <!-- Required for animated icons -->
        <item name="windowSplashScreenAnimationDuration">200</item>

        <!-- Set the theme of the Activity that directly follows your splash screen. -->
        <!-- Required -->
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
    </style>

    <!-- ActionBar styles -->
    <style name="MyActionBar" parent="@style/Widget.MaterialComponents.ActionBar.Solid">
        <item name="android:background">@color/purple</item>

        <!-- Support library compatibility -->
        <item name="background">@color/purple</item>
    </style>

    <!-- Inherits AppTheme's purple palette (parent="AppTheme"), then strips the framework
         ActionBar so each Activity can supply its own Toolbar and draw behind the status bar. -->
    <style name="AppTheme.NoActionBar" parent="AppTheme">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.TabLayout" parent="Widget.MaterialComponents.TabLayout.Colored">
        <item name="tabIndicatorColor">@color/purple_accent_two</item>
        <item name="tabTextColor">@android:color/white</item>
    </style>

    <style name="ToolbarStyle" parent ="Widget.MaterialComponents.Toolbar">
        <item name="android:background">@color/purple</item>
        <item name="titleTextColor">@color/on_purple</item>
    </style>
</resources>
